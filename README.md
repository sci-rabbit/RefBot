# RefBot — Telegram-бот для поиска 3D-референсов

Асинхронный Telegram-бот для поиска визуальных референсов в Telegram-канале по хештегам. Разработан для команды 3D-артистов.

## 🎯 Функционал

- **🔍 Поиск референсов:** Поиск изображений в Telegram-канале по хештегам с поддержкой нескольких тегов
- **🧠 Система предложений:** Пользователи могут отправлять свои референсы на модерацию
- **📦 Группировка медиа:** Автоматическая группировка сообщений в альбомы
- **📄 Пагинация:** Выдача результатов порциями по 50 элементов
- **⚡ Асинхронная обработка:** Оптимизированная загрузка медиа через Telethon с ограничением параллельных запросов
- **🛡️ Rate limiting:** Защита от злоупотреблений через Redis
- **👮 Админ-панель:** Модерация и публикация пользовательских предложений

## 🛠 Технологии

- **Python 3.12** — основной язык
- **Aiogram 3.x** — фреймворк для Telegram-бота
- **Telethon** — клиент для работы с Telegram API
- **Redis** — хранение данных и rate limiting
- **Docker & Docker Compose** — контейнеризация и деплой
- **Poetry** — управление зависимостями
- **Pydantic Settings** — конфигурация через переменные окружения

## 📋 Требования

- Python 3.12+
- Redis 7.4+
- Docker и Docker Compose (для деплоя)
- Telegram Bot Token
- Telegram API credentials (api_id, api_hash)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd RefBot
```

### 2. Установка зависимостей

```bash
poetry install
```

Или через pip:

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Создайте файл `.env` в корне проекта:

```env
BOT__TOKEN=your_bot_token_here
BOT__SOURCE_CHAT=...
BOT__ADMIN_IDS=[..., ...]

TG_CLIENT__API_ID=your_api_id
TG_CLIENT__API_HASH=your_api_hash
TG_CLIENT__SESSION=user

REDIS__HOST=localhost
REDIS__PORT=6379
REDIS__PASSWORD=your_redis_password
REDIS__ONE_WEEK=604800

LIMIT=5000
```

### 4. Запуск через Docker Compose (рекомендуется)

```bash
docker-compose up -d
```

Бот запустится автоматически в контейнере.

### 5. Запуск локально

```bash
# Убедитесь, что Redis запущен
redis-server

# Запустите бота
python run.py
```

## 📁 Структура проекта

```
RefBot/
├── src/
│   ├── bot.py              # Инициализация бота
│   └── tg_client.py        # Telethon клиент
├── views/                  # Обработчики и роутеры
│   ├── start_view/         # Команда /start
│   ├── search_view/        # Логика поиска
│   └── suggestion_view/    # Система предложений
├── core/
│   ├── keyboards/          # Клавиатуры
│   └── states/             # FSM состояния
├── redis_client/
│   └── redis.py            # Redis клиент
├── auth/
│   └── check_admin.py      # Проверка прав администратора
├── config.py               # Конфигурация приложения
├── run.py                  # Точка входа
├── middleware.py           # Middleware (rate limiting)
├── error_handler.py        # Обработка ошибок
├── Dockerfile
├── docker-compose.yaml
└── pyproject.toml
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `BOT__TOKEN` | Токен Telegram-бота | `123456:ABC-DEF...` |
| `BOT__SOURCE_CHAT` | ID канала для поиска | `-1001234567890` |
| `BOT__ADMIN_IDS` | Список ID администраторов | `[123456789]` |
| `TG_CLIENT__API_ID` | Telegram API ID | `12345678` |
| `TG_CLIENT__API_HASH` | Telegram API Hash | `abc123def456...` |
| `REDIS__HOST` | Хост Redis | `localhost` |
| `REDIS__PORT` | Порт Redis | `6379` |
| `REDIS__PASSWORD` | Пароль Redis | `your_password` |
| `LIMIT` | Максимум сообщений для обработки | `5000` |

### Docker Secrets

Для безопасного хранения секретов используйте Docker secrets (см. `docker-compose.yaml`):

```yaml
secrets:
  bot_token:
    file: ./secrets/bot_token.txt
  # ...
```

## 🔍 Использование

### Поиск референсов

1. Отправьте команду `/start` или нажмите кнопку **🔎Поиск**
2. Введите запрос в формате: `#хештег1 #хештег2 ...`
3. Бот найдет все медиа с указанными хештегами
4. Используйте пагинацию для просмотра всех результатов

### Добавление предложений

1. Нажмите кнопку **🧠Предложка** → **Добавить**
2. Отправьте фото или документы (можно медиа-группой)
3. Добавьте подпись (опционально)
4. Администраторы получат уведомление о новой предложке

### Администратор

- **Просмотр:** Просмотр всех предложений пользователей
- **Опубликовать:** Публикация предложки в канал
- **Удалить:** Удаление предложки без публикации

## 🏗 Архитектура

### Поиск

- Использует `Telethon.iter_messages()` для асинхронной итерации по сообщениям канала
- Фильтрация по хештегам через регулярные выражения
- Группировка сообщений по `grouped_id` или `message.id`
- Ограничение: до 5000 сообщений за запрос (настраивается)

### Оптимизация загрузки

- `asyncio.Semaphore(5)` — ограничение параллельных загрузок
- `asyncio.gather()` — конкурентное скачивание медиа из альбомов
- Обработка исключений при загрузке отдельных файлов

### Redis

- **Хранение предложений:** `suggestion:{media_group_id}` с TTL 1 неделя
- **Rate limiting:** `rate:{user_id}` с временным окном (настраивается)
- **Пулинг:** Максимум 20 соединений через `AsyncRedisClient`

## 📝 Разработка

### Установка dev-зависимостей

```bash
poetry install --with dev
```

### Логирование

Централизованное логирование настроено в `run.py`:
- Формат: `%(asctime)s [%(levelname)s] %(name)s:%(lineno)d | %(message)s`
- Уровень: `INFO`

### Обработка ошибок

Глобальный обработчик ошибок регистрируется в `error_handler.py` и отправляет пользователю понятные сообщения об ошибках.

## 🐳 Docker

### Сборка образа

```bash
docker build -t refbot .
```

### Запуск с Docker Compose

```bash
docker-compose up -d        # Запуск в фоне
docker-compose logs -f      # Просмотр логов
docker-compose down         # Остановка
```

### Volumes

- `./logs:/app/logs` — логи приложения
- `redis_data` — данные Redis (persistent)

## 📊 Производительность

- **Параллельные загрузки:** до 5 одновременных через Semaphore
- **Поиск:** до 5000 сообщений за запрос
- **Пагинация:** 50 элементов на страницу
- **Redis connections:** пул из 20 соединений

## 🔒 Безопасность

- Секреты хранятся через Docker secrets
- Проверка прав администратора через фильтр `IsAdmin`
- Rate limiting для защиты от злоупотреблений
- TTL для автоматической очистки данных Redis

